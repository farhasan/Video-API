module.exports = {
    connection : {
        host : "localhost",
        user : "video_user",
        database : "VIDEOS_DB",
        dateStrings: "date" // allows dates to be formatted correctly by default (YYYY-MM-DD)
    }
};